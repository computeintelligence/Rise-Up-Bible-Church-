packages:
  "framer-motion": "For premium page transitions and scroll-triggered animations"
  "react-hook-form": "For form state management"
  "@hookform/resolvers": "For form validation"
  "zod": "For schema validation"

images: []

notes:
  - Frontend-only React application, no backend endpoints required
  - Uses Framer Motion for scroll animations and interactions
  - Content is hardcoded as requested
