{
  "buildCommand": "pnpm -w run typecheck:libs && cd artifacts/rise-up-church && pnpm run build",
  "outputDirectory": "artifacts/rise-up-church/dist/public",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
