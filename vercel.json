{
  "buildCommand": "cd artifacts/rise-up-church && pnpm install --frozen-lockfile && pnpm run build",
  "outputDirectory": "artifacts/rise-up-church/dist/public",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
